#ifndef H_XD_GRAPHICS_DETAIL_IMAGE
#define H_XD_GRAPHICS_DETAIL_IMAGE

#include <memory>

namespace xd { namespace detail { namespace image {

	struct handle;
	typedef std::shared_ptr<handle> handle_ptr;

} } }

#endif
